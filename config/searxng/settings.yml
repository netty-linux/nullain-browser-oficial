use_default_settings: true

general:
  debug: false
  instance_name: "Nullain Site Resolver"

search:
  safe_search: 1
  formats:
    - html
    - json

server:
  secret_key: "nullain-local-site-resolver-not-public"
  limiter: false
  image_proxy: false
  public_instance: false
